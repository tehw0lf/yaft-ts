node_modules
dist

# Fetched by scripts/fetch-conformance.sh from the version pinned in
# conformance.lock. Never committed: the lock file plus its checksum is what
# makes a suite bump a reviewable one-line diff, and a checked-in copy would
# drift from the tag it claims to be.
src/test/conformance/
